import { useCallback, useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { supabase } from '@/integrations/supabase/client';
import { useAuth } from '@/contexts/AuthContext';
import { toast } from 'sonner';
type Kind='application'|'residence';
type Profile={id:string;user_id:string;kind:Kind;slug:string;display_name:string;campus:string;tiktok_url:string|null;status:string;bio_review:string;bio_verified_until:string|null};
type Scope={id:string;profile_id:string;scope_kind:string;scope_value:string;state:string};
export default function PartnerApprovalConsole({kind}:{kind:Kind}){const {user}=useAuth();const [allowed,setAllowed]=useState(false);const [rows,setRows]=useState<Profile[]>([]);const [scopes,setScopes]=useState<Scope[]>([]);const [loading,setLoading]=useState(true);const [busy,setBusy]=useState('');const [alloc,setAlloc]=useState<Record<string,string>>({});const [residences,setResidences]=useState<{id:string;name:string;campus:string|null}[]>([]);const [institutions,setInstitutions]=useState<{id:string;name:string;institution_type:string}[]>([]);
 const load=useCallback(async()=>{if(!user){setAllowed(false);setLoading(false);return;}const permission=await(supabase as any).rpc('rk_partner_staff',{p_kind:kind});if(permission.error||permission.data!==true){setAllowed(false);setLoading(false);return;}setAllowed(true);setLoading(true);const [p,s,options]=await Promise.all([(supabase as any).from('rk_partner_profiles').select('id,user_id,kind,slug,display_name,campus,tiktok_url,status,bio_review,bio_verified_until').eq('kind',kind).order('created_at',{ascending:false}).limit(150),(supabase as any).from('rk_partner_scopes').select('id,profile_id,scope_kind,scope_value,state').limit(1500),kind==='residence'?(supabase as any).from('residences').select('id,name,campus').eq('is_visible',true).order('name').limit(500):(supabase as any).from('institutions').select('id,name,institution_type').eq('is_active',true).order('name').limit(500)]);if(p.error||s.error||options.error){toast.error(p.error?.message||s.error?.message||options.error?.message||'Could not load partner review');}setRows(p.data||[]);setScopes(s.data||[]);if(kind==='residence')setResidences(options.data||[]);else setInstitutions(options.data||[]);setLoading(false);},[user?.id,kind]);useEffect(()=>{void load();},[load]);
 const review=async(p:Profile,status:string,bio:string)=>{if(!allowed)return;const expiry=bio==='verified'?new Date(Date.now()+30*86400000).toISOString():null;if(bio==='verified'&&!window.confirm(`I have opened ${p.tiktok_url||'the TikTok profile'} and confirmed the exact live link to /partners/${p.slug} is in the bio. Grant verification for 30 days?`))return;setBusy(p.id);const {error}=await(supabase as any).rpc('rk_review_partner',{p_profile_id:p.id,p_status:status,p_bio_review:bio,p_bio_expiry:expiry});setBusy('');if(error)return toast.error(error.message);toast.success('Approval decision recorded');await load();};
 const scopeReview=async(s:Scope,state:string)=>{if(!allowed)return;setBusy(s.id);const {error}=await(supabase as any).rpc('rk_review_partner_scope',{p_scope_id:s.id,p_state:state});setBusy('');if(error)return toast.error(error.message);toast.success('Assignment updated');await load();};
 const allocate=async(p:Profile,scopeKind:string,value:string)=>{if(!value)return;setBusy(p.id);const {error}=await(supabase as any).rpc('rk_admin_assign_partner_scope',{p_profile_id:p.id,p_kind:scopeKind,p_value:value,p_state:'approved'});setBusy('');if(error)return toast.error(error.message);toast.success('Assignment approved and audited');setAlloc(v=>({...v,[p.id]:''}));await load();};
 if(!allowed)return null;
 return <Card className="border-primary/20"><CardHeader><CardTitle>{kind==='application'?'Applications':'Residence'} Admin · Partner approvals and assignments</CardTitle><p className="text-sm text-muted-foreground">This workspace requires your assigned department and an AAL2-verified staff session. God Mode staff roles are assigned separately through the Access editor. TikTok verification does not grant student-record access.</p></CardHeader><CardContent className="space-y-4">{loading?<p>Loading partner approvals…</p>:!rows.length?<p className="text-sm text-muted-foreground">No partner profiles have been submitted yet.</p>:rows.map(p=><section key={p.id} className="space-y-3 rounded-xl border p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><h3 className="font-bold">{p.display_name} <Badge variant="outline">{p.status}</Badge></h3><p className="text-xs text-muted-foreground">{p.campus||'No campus'} · <Link className="underline" to={`/partners/${p.slug}`}>Public profile</Link></p>{p.tiktok_url&&<a className="text-xs underline" href={p.tiktok_url} target="_blank" rel="noopener noreferrer">Inspect live TikTok bio</a>}</div><div className="flex flex-wrap gap-2"><Button size="sm" disabled={!!busy} onClick={()=>review(p,'published',p.bio_review==='verified'?'verified':p.bio_review)}>Publish</Button><Button size="sm" variant="outline" disabled={!!busy} onClick={()=>review(p,'paused','unverified')}>Pause</Button><Button size="sm" variant="outline" disabled={!!busy||!p.tiktok_url||p.status!=='published'} onClick={()=>review(p,'published','verified')}>Verify bio · 30 days</Button><Button size="sm" variant="outline" disabled={!!busy} onClick={()=>review(p,p.status,'rejected')}>Reject bio</Button></div></div><div><h4 className="text-sm font-bold">Requested / approved assignments</h4><div className="mt-2 flex flex-wrap gap-2">{scopes.filter(s=>s.profile_id===p.id).map(s=><span key={s.id} className="inline-flex items-center gap-2 rounded-lg border p-2 text-xs">{s.scope_value} · {s.state}{s.state!=='approved'&&<Button size="sm" variant="outline" disabled={!!busy} onClick={()=>scopeReview(s,'approved')}>Approve</Button>}{s.state==='approved'&&<Button size="sm" variant="outline" disabled={!!busy} onClick={()=>scopeReview(s,'paused')}>Pause</Button>}</span>)}</div></div><form className="flex flex-wrap gap-2" onSubmit={e=>{e.preventDefault();const value=alloc[p.id]||'';void allocate(p,kind==='application'?'institution':'residence',value);}}><select className="min-w-[210px] flex-1 rounded-md border bg-background p-2 text-sm" aria-label={`Assign ${kind==='application'?'institution':'residence'} to ${p.display_name}`} value={alloc[p.id]||''} onChange={e=>setAlloc(v=>({...v,[p.id]:e.target.value}))}><option value="">Choose {kind==='application'?'an institution':'a residence'}</option>{kind==='application'?institutions.map(i=><option key={i.id} value={i.name}>{i.name} · {i.institution_type}</option>):residences.map(r=><option key={r.id} value={r.id}>{r.name} · {r.campus||'Area not set'}</option>)}</select><Button disabled={!!busy||!alloc[p.id]}>Assign</Button></form>{kind==='residence'&&<form className="flex gap-2" onSubmit={e=>{e.preventDefault();const v=alloc[p.id+'-campus'];void allocate(p,'campus',v);}}><Input placeholder="Optional shared campus territory" maxLength={150} value={alloc[p.id+'-campus']||''} onChange={e=>setAlloc(v=>({...v,[p.id+'-campus']:e.target.value}))}/><Button variant="outline" disabled={!!busy||!(alloc[p.id+'-campus']||'').trim()}>Assign campus</Button></form>}</section>)}</CardContent></Card>;
}
