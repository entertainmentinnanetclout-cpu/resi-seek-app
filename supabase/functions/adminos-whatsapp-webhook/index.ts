import { serve } from "https://deno.land/std@0.224.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.79.0";

declare const EdgeRuntime:{waitUntil(p:Promise<unknown>):void};
const env=(n:string)=>Deno.env.get(n)||"";
const supabaseUrl=env("SUPABASE_URL")||env("EXTERNAL_SUPABASE_URL");
const serviceKey=env("SUPABASE_SERVICE_ROLE_KEY")||env("EXTERNAL_SUPABASE_SERVICE_ROLE_KEY");
const accountSid=env("TWILIO_ACCOUNT_SID"),authToken=env("TWILIO_AUTH_TOKEN"),fromNumber=env("TWILIO_WHATSAPP_FROM");
const canonicalWebhookUrl=env("TWILIO_WHATSAPP_WEBHOOK_URL"),statusCallback=env("TWILIO_WHATSAPP_STATUS_CALLBACK_URL");
const PUBLIC_BASE="https://www.reskonnect.org";
const link=(p:string)=>`${PUBLIC_BASE}${p.startsWith("/")?p:`/${p}`}`;
const digits=(v="")=>String(v).replace(/^whatsapp:/i,"").replace(/\D/g,"");
const e164=(v="")=>{let d=digits(v);if(d.startsWith("0")&&d.length===10)d=`27${d.slice(1)}`;if(!d.startsWith("27")&&d.length===9)d=`27${d}`;return d?`+${d}`:"";};
const wa=(v="")=>{const n=e164(v);return n?`whatsapp:${n}`:"";};
const norm=(v="")=>digits(e164(v));
const basic=()=>`Basic ${btoa(`${accountSid}:${authToken}`)}`;
const xml=(status=200)=>new Response("<Response></Response>",{status,headers:{"Content-Type":"text/xml; charset=utf-8","Cache-Control":"no-store"}});
const txt=(v:unknown)=>String(v??"").trim();
const first=(v="")=>String(v||"there").trim().split(/\s+/)[0]||"there";
const LANGS=new Set(["en","zu","nso","tn","st","xh","af"]);
const menuForLanguage=(code:string)=>code==="en"?"rk_main_menu_v2":`rk_main_menu_${code}`;

async function hmacSha1Base64(keyText:string,value:string){const enc=new TextEncoder();const key=await crypto.subtle.importKey("raw",enc.encode(keyText),{name:"HMAC",hash:"SHA-1"},false,["sign"]);const sig=await crypto.subtle.sign("HMAC",key,enc.encode(value));let b="";for(const n of new Uint8Array(sig))b+=String.fromCharCode(n);return btoa(b);}
async function verifySignature(req:Request,p:URLSearchParams){if(!authToken)return false;const got=req.headers.get("X-Twilio-Signature")||"";if(!got)return false;const keys=Array.from(new Set(Array.from(p.keys()))).sort();let payload=canonicalWebhookUrl||req.url;for(const k of keys)for(const v of p.getAll(k))payload+=`${k}${v}`;const expected=await hmacSha1Base64(authToken,payload);if(got.length!==expected.length)return false;let diff=0;for(let i=0;i<got.length;i++)diff|=got.charCodeAt(i)^expected.charCodeAt(i);return diff===0;}
async function verifyRest(p:URLSearchParams){try{const sid=p.get("MessageSid")||p.get("SmsSid")||"";if(!sid||p.get("AccountSid")!==accountSid)return false;const r=await fetch(`https://api.twilio.com/2010-04-01/Accounts/${accountSid}/Messages/${encodeURIComponent(sid)}.json`,{headers:{Authorization:basic()}});const d=await r.json();if(!r.ok||d.sid!==sid||d.account_sid!==accountSid)return false;if(p.get("From")&&d.from!==p.get("From"))return false;if(p.get("To")&&d.to!==p.get("To"))return false;if(p.get("Body")!==null&&String(d.body||"")!==String(p.get("Body")||""))return false;return true;}catch{return false;}}
async function twilioSend(form:URLSearchParams){if(!accountSid||!authToken||!fromNumber)throw new Error("Twilio WhatsApp is not configured");if(statusCallback)form.set("StatusCallback",statusCallback);const r=await fetch(`https://api.twilio.com/2010-04-01/Accounts/${accountSid}/Messages.json`,{method:"POST",headers:{Authorization:basic(),"Content-Type":"application/x-www-form-urlencoded"},body:form});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.message||`Twilio HTTP ${r.status}`);return d;}
async function activity(s:any,threadId:string,type:string,metadata:any={}){await s.from("adminos_whatsapp_activity").insert({thread_id:threadId,actor_id:null,event_type:type,metadata});}
async function persistSent(s:any,thread:any,contactId:string|null,to:string,sent:any,body:string|null,metadata:any={},touch=true){const now=new Date().toISOString();const row=await s.from("adminos_whatsapp_messages").upsert({thread_id:thread.id,contact_id:contactId,twilio_message_sid:sent.sid,direction:"outbound",from_address:wa(fromNumber),to_address:wa(to),body_text:body,message_kind:"service",status:sent.status==="queued"?"queued":"sent",risk_level:"green",sent_at:now,metadata:{source:"dimpho_whatsapp",author_type:"ai",persona:"Dimpho",...metadata}},{onConflict:"twilio_message_sid"}).select("id").maybeSingle();if(touch)await s.from("adminos_whatsapp_threads").update({last_message_at:now,last_outbound_at:now,status:"waiting",updated_at:now}).eq("id",thread.id);else await s.from("adminos_whatsapp_threads").update({last_message_at:now,last_outbound_at:now,updated_at:now}).eq("id",thread.id);await activity(s,thread.id,"dimpho.sent",{message_id:row.data?.id||null,twilio_message_sid:sent.sid,...metadata});return row.data;}
async function sendText(s:any,thread:any,contactId:string|null,to:string,body:string,metadata:any={},touch=true){const clipped=body.trim().slice(0,3500);const sent=await twilioSend(new URLSearchParams({From:wa(fromNumber),To:wa(to),Body:clipped}));await persistSent(s,thread,contactId,to,sent,clipped,metadata,touch);return sent;}
async function sendMedia(s:any,thread:any,contactId:string|null,to:string,body:string,url:string,metadata:any={}){const sent=await twilioSend(new URLSearchParams({From:wa(fromNumber),To:wa(to),Body:body.slice(0,3000),MediaUrl:url}));await persistSent(s,thread,contactId,to,sent,body,{...metadata,rich_media:true});return sent;}
async function sendRich(s:any,thread:any,contactId:string|null,to:string,key:string,vars:Record<string,string>={}){const row=(await s.from("adminos_whatsapp_rich_content").select("*").eq("content_key",key).maybeSingle()).data;if(!row?.content_sid)throw new Error(`Rich WhatsApp content ${key} is not ready`);const form=new URLSearchParams({From:wa(fromNumber),To:wa(to),ContentSid:row.content_sid});if(Object.keys(vars).length)form.set("ContentVariables",JSON.stringify(vars));const sent=await twilioSend(form);await persistSent(s,thread,contactId,to,sent,String(row.config?.body||row.display_name),{content_key:key,content_sid:row.content_sid});await s.from("adminos_whatsapp_threads").update({last_menu_key:key,last_menu_at:new Date().toISOString()}).eq("id",thread.id);thread.last_menu_key=key;return sent;}

async function identity(s:any,contactId:string|null,from:string){let contact:any=contactId?(await s.from("adminos_contacts").select("*").eq("id",contactId).maybeSingle()).data:null;let profile:any=null;if(contact?.profile_user_id)profile=(await s.from("profiles").select("id,full_name,email,phone,phone_number,student_number,campus,course,profile_picture_url").eq("id",contact.profile_user_id).maybeSingle()).data||null;if(!profile){const intl=e164(from),local=intl.startsWith("+27")?`0${intl.slice(3)}`:intl;profile=(await s.from("profiles").select("id,full_name,email,phone,phone_number,student_number,campus,course,profile_picture_url").or(`phone.eq.${intl},phone_number.eq.${intl},phone.eq.${local},phone_number.eq.${local}`).limit(1).maybeSingle()).data||null;if(profile&&contactId&&!contact?.profile_user_id){await s.from("adminos_contacts").update({profile_user_id:profile.id,full_name:contact?.full_name||profile.full_name,email:contact?.email||profile.email,student_number:contact?.student_number||profile.student_number,campus:contact?.campus||profile.campus,updated_at:new Date().toISOString()}).eq("id",contactId);contact={...(contact||{}),profile_user_id:profile.id};}}return{contact,profile};}
async function state(s:any,thread:any,patch:any){const next={...(thread.conversation_state||{}),...patch};await s.from("adminos_whatsapp_threads").update({conversation_state:next,intent:patch.intent||thread.intent||null,updated_at:new Date().toISOString()}).eq("id",thread.id);thread.conversation_state=next;if(patch.intent)thread.intent=patch.intent;return next;}
async function setLanguage(s:any,thread:any,contactId:string|null,code:string,source="selected",confidence=1){if(!LANGS.has(code))return;thread.language_code=code;if(contactId){const r=await s.rpc("adminos_set_contact_language",{p_contact_id:contactId,p_language_code:code,p_source:source,p_confidence:confidence});if(r.error)await s.from("adminos_whatsapp_threads").update({language_code:code,updated_at:new Date().toISOString()}).eq("id",thread.id);}else await s.from("adminos_whatsapp_threads").update({language_code:code,updated_at:new Date().toISOString()}).eq("id",thread.id);}
async function phrase(s:any,key:string,code:string){return (await s.from("adminos_i18n_phrases").select("phrase_text").eq("phrase_key",key).eq("language_code",code).maybeSingle()).data?.phrase_text||(await s.from("adminos_i18n_phrases").select("phrase_text").eq("phrase_key",key).eq("language_code","en").maybeSingle()).data?.phrase_text||"Thank you.";}
async function detectLanguage(s:any,thread:any,contactId:string|null,body:string){if(!body.trim()||body.includes(":"))return thread.language_code||"en";const r=await s.rpc("adminos_detect_language",{p_text:body});const code=LANGS.has(String(r.data||""))?String(r.data):"en";if(code!=="en"&&code!==thread.language_code)await setLanguage(s,thread,contactId,code,"detected",0.85);return code!=="en"?code:(thread.language_code||"en");}
function selectionFrom(p:URLSearchParams,body:string){const direct=txt(p.get("ButtonPayload"));if(direct)return direct;const raw=txt(p.get("InteractiveData"));if(raw){try{const d=JSON.parse(raw);return txt(d?.id||d?.payload||d?.button_payload||d?.selected_id)||body;}catch{}}return body;}
const greeting=(v:string)=>/^(hi|hii+|hello|hey|good\s*(morning|afternoon|evening)|menu|start|help|howzit|dumelang|dumela|sawubona|molo|hallo)[!.\s]*$/i.test(v.trim());
const accommodation=(v:string)=>/\b(accommodation|student res|residence|room|rent|nsfas|tut accommodation|tvet|college accommodation|private tenant|2026 accommodation|2027 accommodation|indawo yokuhlala|madulo|bonno|bolulo|verblyf)\b/i.test(v);
const company=(v:string)=>/\b(about reskonnect|what (do|does) (you|reskonnect) do|your services|company services|services do you offer|tell me about reskonnect|who are you)\b/i.test(v);
function inferAccommodation(v:string,current:any={}){const x=v.toLowerCase(),p:any={...current,intent:"accommodation"};if(/\b2027\b/.test(x))p.year=2027;else if(/\b2026\b/.test(x))p.year=2026;if(/\bnsfas\b/.test(x))p.funding="nsfas";else if(/bursary|sponsor/.test(x))p.funding="bursary";else if(/private funded|self[- ]?funded|cash/.test(x))p.funding="private";if(/\btut\b/.test(x))p.institution="tut";else if(/tvet|college/.test(x))p.institution="tvet";if(/private tenant|non[- ]?student/.test(x))p.tenant="private";else if(/\bstudent\b/.test(x))p.tenant="student";return p;}
async function residenceMatches(s:any,thread:any,contactId:string|null,from:string,st:any){let rows=(await s.from("residences").select("id,name,slug,campus,city,address,price,private_price,nsfas_price,available_spots,accepts_tvet,accepts_university,accepts_private,accepts_nsfas,is_tut_accredited,reservations_2027_open,cover_image_url,image_url,images,is_spotlight,is_featured,featured,trusted,is_trusted,data_quality_score,service_ready").or("is_visible.eq.true,is_visible.is.null").limit(500)).data||[];rows=rows.filter((r:any)=>Number(r.available_spots??1)!==0);if(Number(st.year)===2027)rows=rows.filter((r:any)=>r.reservations_2027_open===true);if(st.institution==="tut")rows=rows.filter((r:any)=>r.is_tut_accredited||r.accepts_university);if(st.institution==="tvet")rows=rows.filter((r:any)=>r.accepts_tvet);if(st.funding==="nsfas")rows=rows.filter((r:any)=>r.accepts_nsfas);if(st.funding==="private"||st.tenant==="private")rows=rows.filter((r:any)=>r.accepts_private);rows.sort((a:any,b:any)=>Number(Boolean(b.is_spotlight))-Number(Boolean(a.is_spotlight))||Number(Boolean(b.is_featured||b.featured))-Number(Boolean(a.is_featured||a.featured))||Number(Boolean(b.service_ready))-Number(Boolean(a.service_ready))||Number(b.data_quality_score||0)-Number(a.data_quality_score||0));rows=rows.slice(0,5);if(!rows.length){await sendText(s,thread,contactId,from,`I couldn't find a published residence matching every selected filter. Explore all current options: ${link("/findmyres")}`,{intent:"accommodation"});return;}await sendText(s,thread,contactId,from,`I found ${rows.length} strong match${rows.length===1?"":"es"} using the latest ResKonnect residence data.`,{intent:"accommodation",result_count:rows.length});for(const r of rows){const price=st.funding==="nsfas"&&r.nsfas_price?r.nsfas_price:(st.funding==="private"&&r.private_price?r.private_price:r.price);const location=[r.campus,r.city].filter(Boolean).join(" · ")||r.address||"Location on listing";const body=[`🏠 ${r.name}`,`📍 ${location}`,price?`💳 R${Number(price).toLocaleString("en-ZA")}`:null,r.available_spots!=null?`Availability: ${r.available_spots} spot${Number(r.available_spots)===1?"":"s"}`:null,`View & apply: ${link(`/find-my-res/${r.slug||r.id}`)}`].filter(Boolean).join("\n");const image=txt(r.cover_image_url||r.image_url||(Array.isArray(r.images)?r.images[0]:""));if(image)await sendMedia(s,thread,contactId,from,body,image,{residence_id:r.id});else await sendText(s,thread,contactId,from,body,{residence_id:r.id,media_missing:true});}}
async function applicationStatus(s:any,thread:any,contactId:string|null,from:string,profile:any){if(!profile?.id){await sendText(s,thread,contactId,from,`Link this WhatsApp number to your ResKonnect profile to securely track your application: ${link("/my-applications")}`);return;}const app=(await s.from("applications").select("id,status,funding_type,move_in_date,residences(name,slug,cover_image_url,image_url)").eq("user_id",profile.id).order("created_at",{ascending:false}).limit(1)).data?.[0];if(!app){await sendText(s,thread,contactId,from,`I don't see an accommodation application linked to this profile yet. Start here: ${link("/findmyres")}`);return;}const r:any=app.residences||{};const body=[`Application status: ${app.status||"Submitted"}`,r.name?`Residence: ${r.name}`:null,app.funding_type?`Funding: ${app.funding_type}`:null,`Manage: ${link("/my-applications")}`].filter(Boolean).join("\n");const image=txt(r.cover_image_url||r.image_url);if(image)await sendMedia(s,thread,contactId,from,body,image,{application_id:app.id});else await sendText(s,thread,contactId,from,body,{application_id:app.id});}
async function missingDocs(s:any,thread:any,contactId:string|null,from:string,profile:any){if(!profile?.id){await sendText(s,thread,contactId,from,`Link this WhatsApp number to your profile first: ${link("/my-applications")}`);return;}const app=(await s.from("applications").select("id").eq("user_id",profile.id).order("created_at",{ascending:false}).limit(1)).data?.[0];if(!app){await sendText(s,thread,contactId,from,`No accommodation application is linked to this profile yet: ${link("/findmyres")}`);return;}const docs=(await s.from("application_documents").select("doc_type,status,rejection_reason").eq("application_id",app.id).in("status",["rejected","missing","invalid","needs_action"])).data||[];if(!docs.length)await sendText(s,thread,contactId,from,`I don't see a recorded document issue on your latest application. Verify the checklist here: ${link("/my-applications")}`);else await sendText(s,thread,contactId,from,`Your application needs attention on:\n${docs.slice(0,8).map((d:any)=>`• ${d.doc_type}: ${d.status}${d.rejection_reason?` — ${d.rejection_reason}`:""}`).join("\n")}\n\nUpload sensitive documents securely: ${link("/my-applications")}`);}
async function wilStatus(s:any,thread:any,contactId:string|null,from:string,profile:any){if(!profile?.id){await sendText(s,thread,contactId,from,`Link this WhatsApp number to your ResKonnect profile to track WIL: ${link("/wil")}`);return;}const w=(await s.from("wil_applications").select("id,status,course,wil_duration,funding_status,campus").eq("student_id",profile.id).order("created_at",{ascending:false}).limit(1)).data?.[0];if(!w){await sendText(s,thread,contactId,from,`I don't see a WIL application linked to this profile. Explore opportunities: ${link("/opportunities/wil")}`);return;}await sendText(s,thread,contactId,from,[`WIL status: ${w.status}`,w.course?`Course: ${w.course}`:null,w.campus?`Campus: ${w.campus}`:null,w.wil_duration?`Duration: ${w.wil_duration}`:null,`Manage WIL: ${link("/wil")}`].filter(Boolean).join("\n"),{wil_application_id:w.id});}
async function handoff(s:any,thread:any,contactId:string|null,from:string,reason:string){await s.from("adminos_whatsapp_threads").update({status:"escalated",mode:"escalated",priority:"high",intent:"human",updated_at:new Date().toISOString()}).eq("id",thread.id);await s.from("adminos_automation_events").insert({event_type:"whatsapp.escalated",entity_type:"whatsapp_thread",entity_id:thread.id,contact_id:contactId,payload:{reason,risk:"amber",persona:"Dimpho"}});await sendRich(s,thread,contactId,from,"rk_handoff_menu");}

async function routeDeterministic(s:any,input:any){const{thread,contactId,from,body,messageId}=input;const selection=txt(input.selection||body),lower=selection.toLowerCase();const who=await identity(s,contactId,from);const name=first(who.profile?.full_name||who.contact?.full_name||"there");const language=await detectLanguage(s,thread,contactId,body);
  if(lower.startsWith("csat:")){const score=Number(lower.split(":")[1]);if(score>=1&&score<=5){const r=await s.rpc("adminos_record_csat",{p_thread_id:thread.id,p_score:score,p_source_message_id:messageId,p_comment:null,p_language:language});if(r.error)throw r.error;await sendText(s,thread,contactId,from,await phrase(s,"csat_thanks",language),{intent:"csat",score},false);await activity(s,thread.id,"csat.recorded",{score,language});return true;}}
  if(lower==="menu:language"||/^(language|change language|puo|ulimi|taal)$/i.test(body.trim())){await sendRich(s,thread,contactId,from,"rk_language_menu");return true;}
  if(lower.startsWith("lang:")){const code=lower.split(":")[1];if(LANGS.has(code)){await setLanguage(s,thread,contactId,code,"selected",1);await sendText(s,thread,contactId,from,await phrase(s,"thanks",code),{intent:"language",language:code});await sendRich(s,thread,contactId,from,menuForLanguage(code),{"1":name});return true;}}
  if(/^(stop|unsubscribe|cancel messages|opt out)$/i.test(body.trim())){if(contactId){const existing=(await s.from("adminos_communication_preferences").select("id").eq("contact_id",contactId).maybeSingle()).data;if(existing)await s.from("adminos_communication_preferences").update({do_not_contact:true,whatsapp_allowed:false,updated_at:new Date().toISOString()}).eq("id",existing.id);else await s.from("adminos_communication_preferences").insert({contact_id:contactId,do_not_contact:true,whatsapp_allowed:false});}await sendText(s,thread,contactId,from,"You're opted out of ResKonnect WhatsApp automation.",{intent:"opt_out"});return true;}
  if(lower==="menu:main"||greeting(body)){await s.from("adminos_whatsapp_threads").update({mode:"ai_auto",status:"open",priority:"normal",intent:"main",conversation_state:{},updated_at:new Date().toISOString()}).eq("id",thread.id);await sendRich(s,thread,contactId,from,menuForLanguage(language),{"1":name});return true;}
  if(lower==="human:wait"){await s.from("adminos_whatsapp_threads").update({mode:"human",status:"escalated",priority:"high",updated_at:new Date().toISOString()}).eq("id",thread.id);await sendText(s,thread,contactId,from,await phrase(s,"human_wait",language),{intent:"human"});return true;}
  if(lower==="menu:human"||lower==="menu:partnerships"||/\b(partnership|partner with|business proposal|residence owner|landlord partnership|collaboration|sponsor|sponsorship|speak to (the )?(owner|director|manager|human|agent))\b/i.test(body)){await handoff(s,thread,contactId,from,lower==="menu:partnerships"?"partnership_or_business_enquiry":"human_requested");return true;}
  if(lower==="menu:applications"){await state(s,thread,{intent:"applications"});await sendRich(s,thread,contactId,from,"rk_application_menu_v3");return true;}
  if(lower==="app:status"){await applicationStatus(s,thread,contactId,from,who.profile);await sendRich(s,thread,contactId,from,"rk_application_menu_v3");return true;}
  if(lower==="app:missing"){await missingDocs(s,thread,contactId,from,who.profile);await sendRich(s,thread,contactId,from,"rk_application_menu_v3");return true;}
  const appLinks:Record<string,string>={"app:university":"/applications/university","app:tvet":"/applications/tvet","app:private-college":"/applications/private-college","app:checker":"/applications/checker","app:start":"/apply","app:upload":"/my-applications"};if(appLinks[lower]){await sendText(s,thread,contactId,from,`Continue securely on ResKonnect: ${link(appLinks[lower])}`,{intent:"applications",action:lower});await sendRich(s,thread,contactId,from,"rk_application_menu_v3");return true;}
  if(lower==="app:help"){await sendText(s,thread,contactId,from,"Choose the closest application option, or type the exact issue and Dimpho will guide you.");await sendRich(s,thread,contactId,from,"rk_application_menu_v3");return true;}
  if(lower==="menu:opportunities"){await state(s,thread,{intent:"wil"});await sendRich(s,thread,contactId,from,"rk_wil_menu_v3");return true;}
  if(lower==="wil:status"){await wilStatus(s,thread,contactId,from,who.profile);await sendRich(s,thread,contactId,from,"rk_wil_menu_v3");return true;}
  const wilLinks:Record<string,string>={"wil:apply":"/opportunities/wil","wil:documents":"/wil","wil:opportunities":"/opportunities"};if(wilLinks[lower]){await sendText(s,thread,contactId,from,`Continue here: ${link(wilLinks[lower])}`,{intent:"wil",action:lower});await sendRich(s,thread,contactId,from,"rk_wil_menu_v3");return true;}
  if(lower==="wil:requirements"||lower==="wil:placement"||lower==="wil:funding"){await sendText(s,thread,contactId,from,"Tell me your institution, course and the exact WIL question. Dimpho will use verified information and escalate protected placement decisions rather than guess.",{intent:"wil",action:lower});return true;}
  if(lower==="wil:employer"){await handoff(s,thread,contactId,from,"wil_employer_or_host_company");return true;}
  if(lower==="menu:company"||lower==="menu:other"||company(body)){await state(s,thread,{intent:"company"});await sendRich(s,thread,contactId,from,"rk_company_menu");return true;}
  if(lower==="company:about"){await sendText(s,thread,contactId,from,`ResKonnect connects Living, Applications and Opportunities in one platform. Explore: ${PUBLIC_BASE}`);await sendRich(s,thread,contactId,from,"rk_company_menu");return true;}
  if(lower==="company:living"){await sendText(s,thread,contactId,from,`Accommodation and residence discovery: ${link("/living")}`);await sendRich(s,thread,contactId,from,"rk_accommodation_menu");return true;}
  if(lower==="company:applications"){await sendRich(s,thread,contactId,from,"rk_application_menu_v3");return true;}
  if(lower==="company:opportunities"){await sendRich(s,thread,contactId,from,"rk_wil_menu_v3");return true;}
  if(lower==="company:property"){await sendText(s,thread,contactId,from,`Property owners and residence operators: ${link("/partners/landlords")}`);await sendRich(s,thread,contactId,from,"rk_company_menu");return true;}
  if(lower==="company:ai"){await sendText(s,thread,contactId,from,`ResKonnect uses automation and AI for faster support while protected decisions remain human-controlled. ${PUBLIC_BASE}`);await sendRich(s,thread,contactId,from,"rk_company_menu");return true;}
  if(lower==="menu:technical"){await state(s,thread,{intent:"technical"});await sendRich(s,thread,contactId,from,"rk_support_menu");return true;}
  if(lower==="support:account"){await sendText(s,thread,contactId,from,`Use the secure sign-in/password-reset flow: ${link("/auth")} — never send passwords or OTPs in chat.`);return true;}
  if(lower==="support:technical"){await sendText(s,thread,contactId,from,"Tell me the page, what you expected and the exact error. Dimpho will troubleshoot routine issues and escalate account-level fixes with context.");return true;}
  if(lower==="menu:reservations"||lower==="menu:accommodation"){await state(s,thread,{intent:"accommodation"});await sendRich(s,thread,contactId,from,"rk_accommodation_menu");return true;}
  if(lower==="acc:find"){await state(s,thread,{intent:"accommodation"});await sendRich(s,thread,contactId,from,"rk_year_menu");return true;}
  if(lower.startsWith("acc:year:")){await state(s,thread,{intent:"accommodation",year:Number(lower.split(":")[2])||null});await sendRich(s,thread,contactId,from,"rk_funding_menu");return true;}
  if(lower.startsWith("acc:institution:")){await state(s,thread,{intent:"accommodation",institution:lower.split(":")[2]});await sendRich(s,thread,contactId,from,"rk_funding_menu");return true;}
  if(lower.startsWith("acc:funding:")){await state(s,thread,{intent:"accommodation",funding:lower.split(":")[2]});await sendRich(s,thread,contactId,from,"rk_tenant_menu");return true;}
  if(lower.startsWith("acc:tenant:")){const st=await state(s,thread,{intent:"accommodation",tenant:lower.split(":")[2]});await residenceMatches(s,thread,contactId,from,st);await sendRich(s,thread,contactId,from,"rk_accommodation_menu");return true;}
  if(accommodation(body)){const st=await state(s,thread,inferAccommodation(body,thread.conversation_state||{}));if(st.year&&st.funding&&(st.institution||st.tenant))await residenceMatches(s,thread,contactId,from,st);else await sendRich(s,thread,contactId,from,"rk_accommodation_menu");return true;}
  if(/\b(application|aps|university application|college application|missing document|documents missing)\b/i.test(body)){await sendRich(s,thread,contactId,from,"rk_application_menu_v3");return true;}
  if(/\b(wil|work integrated learning|internship|placement|host company)\b/i.test(body)){await sendRich(s,thread,contactId,from,"rk_wil_menu_v3");return true;}
  if((input.mediaCount||0)>0&&!body.trim()){await sendText(s,thread,contactId,from,"I received your attachment and AdminOS is securing a private copy for the ResKonnect team. Tell me what you need help with; do not send passwords, OTPs or banking credentials.");await sendRich(s,thread,contactId,from,menuForLanguage(language),{"1":name});return true;}
  return false;
}

async function processInbound(s:any,input:any){const{thread,contactId,from,body,messageId,authMode}=input;try{const prefs=contactId?(await s.from("adminos_communication_preferences").select("do_not_contact,whatsapp_allowed").eq("contact_id",contactId).maybeSingle()).data:null;if((prefs?.do_not_contact||prefs?.whatsapp_allowed===false)&&!/^(start|subscribe|resume)$/i.test(body.trim()))return;if(await routeDeterministic(s,input))return;const mode=thread.mode||"ai_auto";if(mode==="human"){await s.from("adminos_automation_events").insert({event_type:"whatsapp.human_mode_waiting",entity_type:"whatsapp_thread",entity_id:thread.id,contact_id:contactId,payload:{message_id:messageId}});return;}if(mode==="escalated"){await sendRich(s,thread,contactId,from,"rk_handoff_menu");return;}const language=thread.language_code||"en";const history=(await s.from("adminos_whatsapp_messages").select("direction,body_text,metadata,created_at").eq("thread_id",thread.id).order("created_at",{ascending:false}).limit(24)).data||[];const who=await identity(s,contactId,from);const r=await fetch(`${supabaseUrl}/functions/v1/adminos-agent`,{method:"POST",headers:{Authorization:`Bearer ${serviceKey}`,apikey:serviceKey,"Content-Type":"application/json"},body:JSON.stringify({action:"public_enquiry",message:body,contact_id:contactId,context:{channel:"whatsapp",preferred_language:language,verified_phone_contact:Boolean(contactId),conversation_history:history.reverse(),customer:who.contact||who.profile||null,conversation_state:thread.conversation_state||{},canonical_website:PUBLIC_BASE,instruction:`You are Dimpho, ResKonnect's premium service assistant. Respond naturally in the preferred language ${language}. Use verified context only. Do not make a separate translation call. All ResKonnect links must use https://www.reskonnect.org. Never invent availability, pricing, application status, funding outcomes or policy. Never request passwords, OTPs, banking details, identity numbers or sensitive documents in open WhatsApp. Escalate protected decisions, safety/scam/legal matters, owner-level partnerships and unresolved exceptions, but always acknowledge the user.`}})});const agent=await r.json().catch(()=>({}));if(!r.ok||!agent.answer)throw new Error(agent.error||`Agent HTTP ${r.status}`);const answer=String(agent.answer).replaceAll("https://reskonnect.org",PUBLIC_BASE).slice(0,2000);if(mode==="assist"){await s.from("adminos_whatsapp_drafts").update({status:"superseded",updated_at:new Date().toISOString()}).eq("thread_id",thread.id).eq("status","ready");await s.from("adminos_whatsapp_drafts").insert({thread_id:thread.id,source_message_id:messageId,body_text:answer,status:"ready",risk_level:["green","amber","red"].includes(agent.risk)?agent.risk:"amber",confidence:agent.confidence||null,agent_run_id:agent.run_id||null,metadata:{source:"assist_auto_draft",persona:"Dimpho"}});return;}if(agent.risk!=="red"&&!agent.escalate){await sendText(s,thread,contactId,from,answer,{risk:agent.risk||"green",confidence:agent.confidence||null,agent_run_id:agent.run_id||null});return;}await sendText(s,thread,contactId,from,answer||"Thanks — this needs a ResKonnect team member. I have passed the conversation context to them.");await handoff(s,thread,contactId,from,agent.reason||"agent_requested_escalation");}catch(e){const reason=e instanceof Error?e.message:String(e);await sendText(s,thread,contactId,from,"Thanks — I have your message. I couldn't complete this safely through automation, so I have escalated it with the conversation context attached.").catch(()=>null);await s.from("adminos_whatsapp_threads").update({status:"escalated",mode:"escalated",priority:"high",updated_at:new Date().toISOString()}).eq("id",thread.id);await activity(s,thread.id,"system.error",{reason,message_id:messageId});}}

serve(async req=>{if(req.method!=="POST")return xml(405);if(!supabaseUrl||!serviceKey)return xml(500);const raw=await req.text(),p=new URLSearchParams(raw);const signed=await verifySignature(req,p),rest=signed?false:await verifyRest(p);if(!signed&&!rest)return xml(403);const authMode=signed?"signature":"rest_fallback";const s=createClient(supabaseUrl,serviceKey,{auth:{persistSession:false}});const sid=p.get("MessageSid")||p.get("SmsSid")||"",status=p.get("MessageStatus")||p.get("SmsStatus")||"",from=p.get("From")||"",to=p.get("To")||"",body=(p.get("Body")||"").slice(0,12000);if(sid&&status&&!body.trim()&&["queued","sent","delivered","read","failed","undelivered"].includes(status)){const patch:any={status};if(status==="delivered"||status==="read")patch.delivered_at=new Date().toISOString();await s.from("adminos_whatsapp_messages").update(patch).eq("twilio_message_sid",sid);await s.from("adminos_whatsapp_outbox").update({status:["failed","undelivered"].includes(status)?"failed":"sent",last_error:["failed","undelivered"].includes(status)?(p.get("ErrorMessage")||p.get("ErrorCode")||status):null}).eq("twilio_message_sid",sid);return xml();}if(!sid||!from)return xml();const address=norm(from);if(!address)return xml();const resolved=await s.rpc("adminos_resolve_contact",{p_full_name:p.get("ProfileName")||null,p_email:null,p_phone:e164(from),p_profile_user_id:null,p_source_type:"whatsapp",p_source_id:null,p_metadata:{source:"twilio_whatsapp_inbound",twilio_sid:sid}});const contactId=resolved.data||null;let thread=(await s.from("adminos_whatsapp_threads").select("*").eq("normalized_address",address).maybeSingle()).data;if(!thread){const created=await s.from("adminos_whatsapp_threads").insert({contact_id:contactId,channel_address:wa(from),normalized_address:address,status:"open",mode:"ai_auto",language_code:"en",metadata:{source:"twilio"}}).select("*").single();thread=created.data;}if(!thread)return xml();const mediaCount=Math.max(0,Math.min(10,Number(p.get("NumMedia")||0))),media:any[]=[];for(let i=0;i<mediaCount;i++)media.push({url:p.get(`MediaUrl${i}`),content_type:p.get(`MediaContentType${i}`),name:`WhatsApp attachment ${i+1}`});const selection=selectionFrom(p,body),isCsat=/^csat:[1-5]$/i.test(selection);const now=new Date(),nowIso=now.toISOString(),expires=new Date(now.getTime()+86400000).toISOString();const inserted=await s.from("adminos_whatsapp_messages").upsert({thread_id:thread.id,contact_id:contactId,twilio_message_sid:sid,direction:"inbound",from_address:wa(from),to_address:wa(to),body_text:body||p.get("ButtonText")||selection||null,media,message_kind:"service",status:"received",received_at:nowIso,metadata:{account_sid:p.get("AccountSid"),profile_name:p.get("ProfileName")||null,author_type:"contact",auth_mode:authMode,button_payload:p.get("ButtonPayload")||null,button_text:p.get("ButtonText")||null,interactive_data:p.get("InteractiveData")||null,selection}} ,{onConflict:"twilio_message_sid"}).select("id").maybeSingle();if(!inserted.data)return xml();const reopened=thread.mode==="closed"&&!isCsat?"ai_auto":(thread.mode||"ai_auto");const patch:any={contact_id:contactId||thread.contact_id,last_message_at:nowIso,last_inbound_at:nowIso,customer_window_expires_at:expires,unread_count:Number(thread.unread_count||0)+1,updated_at:nowIso};if(!isCsat)Object.assign(patch,{status:"open",mode:reopened,resolved_at:null,resolved_by:null});await s.from("adminos_whatsapp_threads").update(patch).eq("id",thread.id);await s.from("adminos_automation_events").insert({event_type:"whatsapp.received",entity_type:"whatsapp_thread",entity_id:thread.id,contact_id:contactId,payload:{message_id:inserted.data.id,message_sid:sid,body:body.slice(0,1000),selection,media_count:mediaCount,auth_mode:authMode}});await activity(s,thread.id,"message.received",{message_id:inserted.data.id,message_sid:sid,selection,media_count:mediaCount});EdgeRuntime.waitUntil(processInbound(s,{thread:{...thread,mode:reopened,customer_window_expires_at:expires},contactId,from,body:body||p.get("ButtonText")||selection||"",selection,messageId:inserted.data.id,authMode,mediaCount}));return xml();});